# Maintained in catalog/website/ (generated by scripts/update-plugin-website.mjs on first resolve; this file wins over any plugin-repo copy).
# Hand-edited entry VALUES are preserved forever, but comments and formatting are normalized whenever any entry regenerates.

display_name = "Dispatch"
tagline = "Delegate tasks to external agent CLIs through one router and one brief contract: probed facts, write isolation, a multi-model audit panel, and concurrent image generation with failure memory"
repo = "WeZZard/dispatch"
source_hash = "d6e581511ef491e9"
content_hash = "a486ed12a0d1e41e"
