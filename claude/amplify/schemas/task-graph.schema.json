{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/WeZZard/skills/claude/amplify/task-graph.schema.json",
  "title": "Amplify Folded Task Graph",
  "description": "The folded (human-authored, machine-dumped) task graph that scripts/task.mjs ingests. Each node is one 'implement-and-audit' task; the engine explodes it into <id>.impl and <id>.audit subnodes. Each subnode's executor (the subagent that runs it) IS stored here via impl.executor / audit.executor; the implementer/auditor model and tools are NOT stored -- execute-plan chooses them at spawn time per the role design guidelines, and each custom executor supplies its own tools via its frontmatter.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "nodes"],
  "properties": {
    "version": {
      "description": "Schema version. Only 1 is supported.",
      "const": 1
    },
    "nodes": {
      "description": "The folded tasks. Task ids must be unique; every dep must reference an existing task id; the dependency graph must be acyclic.",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/task" }
    }
  },
  "$defs": {
    "task": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "deps", "acceptance_criteria", "audit", "max_attempts"],
      "properties": {
        "id": {
          "description": "Unique task id. Must not contain '.', which the engine reserves as the subnode separator (<id>.impl / <id>.audit).",
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]+$",
          "minLength": 1
        },
        "name": {
          "description": "Short human-readable task title.",
          "type": "string",
          "minLength": 1
        },
        "deps": {
          "description": "Task ids this task depends on. A successor's implementer waits until every dependency's auditor has passed.",
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$" }
        },
        "acceptance_criteria": {
          "description": "Independently checkable statements that define done. The auditor verifies each against evidence.",
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "impl": {
          "description": "Implementer subnode config. Optional; a missing impl (or missing impl.executor) defaults to subagent(general-purpose).",
          "type": "object",
          "additionalProperties": false,
          "properties": { "executor": { "$ref": "#/$defs/executor" } }
        },
        "audit": {
          "description": "Auditor subnode config. Replaces audit_level.",
          "type": "object",
          "additionalProperties": false,
          "required": ["executor"],
          "properties": { "executor": { "$ref": "#/$defs/executor" } }
        },
        "human_gate": {
          "description": "Optional. When true, this task requires a human verification gate before it is considered complete.",
          "type": "boolean"
        },
        "max_attempts": {
          "description": "Maximum implement->audit attempts before the task is marked 'failed' (logged, non-halting).",
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "executor": {
      "description": "Names the subagent that runs a subnode. Grammar: subagent(<name>). The subagent(...) wrapper reserves room for future executor kinds.",
      "type": "string",
      "pattern": "^subagent\\((general-purpose|explore|plan|amplify:codex-driver|amplify:kimi-driver|amplify:chrome-devtools-driver|amplify:playwright-driver|amplify:computer-use)\\)$"
    }
  }
}
