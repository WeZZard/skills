{
  "version": 1,
  "plan_file": "(amplify divide-and-conquer Mode B example — wide-research; no session plan)",
  "variables": {},
  "nodes": [
    {
      "id": "seed",
      "type": "fn",
      "deps": [],
      "module": "./fns/seed.mjs",
      "export": "seed",
      "output_schema": { "type": "array" }
    },
    {
      "id": "research",
      "type": "expand",
      "deps": ["seed"],
      "over": "seed",
      "template": {
        "type": "agent",
        "executor": "subagent(general-purpose)",
        "prompt": "You research ONE subtopic in its own isolated context. Read the single subtopic bound to this node by reference (resolve-context --inputs gives you your input handle), research it, and return a concise findings string for that subtopic only. Return only the handle/status; your output is recorded by reference.",
        "output_schema": { "type": "string" },
        "max_attempts": 2
      },
      "gather": "gather"
    },
    {
      "id": "gather",
      "type": "fn",
      "deps": ["research"],
      "module": "./fns/gather.mjs",
      "export": "gather",
      "output_schema": { "type": "array" },
      "require": "all-resolved"
    }
  ]
}
